lst = [1, 2, 3, 99, 100]  # Початковий список

if len(lst) == 0:
    new_lst = [[], []]  # Порожній список
else:
    # Знаходимо середину (перший список більший, якщо кількість непарна)
    mid = (len(lst) + 1) // 2
    new_lst = [lst[:mid], lst[mid:]]

print(new_lst)
